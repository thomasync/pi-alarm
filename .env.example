# Authorization token
TOKEN=None

# Name to display in challenges
NAME="Thomas"

# Debug mode (Activate the logs)
DEBUG=True

# Enable fadein for the first sound
FADE_IN=False

# Maximum duration of a sound [None to disable]
MAX_DURATION_SOUND=60

# Define the system volume
DEFAULT_VOLUME=10

# Define the maximum volume of the system in insane mode
MAXIMUM_VOLUME=20

# Define the delay before the alarm goes to maximum_volume [None to disable]
INSANE_MODE_DELAY=60